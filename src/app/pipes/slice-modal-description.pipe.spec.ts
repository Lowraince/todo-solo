import { SliceModalDescriptionPipe } from './slice-modal-description.pipe';

describe('SliceModalDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new SliceModalDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});

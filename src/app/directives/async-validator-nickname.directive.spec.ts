import { AsyncValidatorNicknameDirective } from './async-validator-nickname.directive';

describe('AsyncValidatorNicknameDirective', () => {
  it('should create an instance', () => {
    const directive = new AsyncValidatorNicknameDirective();
    expect(directive).toBeTruthy();
  });
});
